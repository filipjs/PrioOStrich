Tutorial:
1) TODO


Required SLURM support (checked as of 14.03):
1) finished jobs won't be deleted before reading final data
	- configuration parameter 'MinJobAge'
2) jobs are removed on partition removal / partition name change
	- OK
3) "priority_p_set" calls inside slurmctld_lock_t (write jobs)
	- OK
4) each job_update triggers a call to "priority_p_set"
	- OK
5) time limit/account of running jobs cannot be increased/changed
	- OK (only SlurmUser can do it)
6) priority_array must be created manually
	- OK (build in plugin, destroyed in job_update)
7) prio_factors aren't altered outside of plugin
	- OK


TODO:
- jesli nie ma enforce_part wtedy 'failed' prace sa wlaczane do kampani

- job update bedzie ustawial prio na 0/1.. nie zmieniac prio po updacie??

- sprwdzic overflow, szczegolnie przy TIME * CPUS i wszedzie gdzie jest DOUBLE
  - sprawdzic dzielania przez double (aka czy nie ma gdzies przez 0)

- prio factors beda wskazywaly na losowa partycje - co z tym zrobic???

- QOS_FLAG_ENFORCE_USAGE_THRES - ta flaga nie moze byc wlaczona [qos_ptr->flags], TODO SPRAWDZIC INNE

